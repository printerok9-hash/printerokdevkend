PORT=4000
NODE_ENV=development
SITE_ORIGIN=http://localhost:3000
MONGODB_URI=mongodb://127.0.0.1:27017/pinterok
ADMIN_EMAIL=printerok9@gmail.com
# Supply a unique password of at least 14 characters. Used only to create the first admin.
ADMIN_PASSWORD=
CALENDLY_WEBHOOK_SIGNING_KEY=
# Keep the token private. Required for the one-time webhook registration command.
CALENDLY_ACCESS_TOKEN=
# Public HTTPS backend address plus /api/webhooks/calendly (localhost will not work).
CALENDLY_WEBHOOK_URL=
# Set only to the number of trusted reverse proxies in your deployment.
TRUST_PROXY=

# Activate this address using the confirmation email from FormSubmit.
FORMSUBMIT_EMAIL=printerok9@gmail.com
